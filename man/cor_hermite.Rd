% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cor_hermite.R
\name{cor_hermite}
\alias{cor_hermite}
\alias{cor_hermite.default}
\alias{cor_hermite.matrix}
\alias{cor_hermite.data.frame}
\title{Hermite-Mehler Distribution-Robust Pearson Correlation}
\usage{
cor_hermite(x, ...)

\method{cor_hermite}{default}(
  x,
  y,
  poly_degree = 3L,
  monotonicity = c("relaxed", "strict", "none"),
  ties_method = c("average", "random"),
  conf_level = NULL,
  ci_method = c("fisher", "bootstrap"),
  B = 1000L,
  ...
)

\method{cor_hermite}{matrix}(
  x,
  poly_degree = 3L,
  monotonicity = c("relaxed", "strict", "none"),
  ties_method = c("average", "random"),
  ...
)

\method{cor_hermite}{data.frame}(
  x,
  poly_degree = 3L,
  monotonicity = c("relaxed", "strict", "none"),
  ties_method = c("average", "random"),
  ...
)
}
\arguments{
\item{x}{A numeric vector, matrix, or data.frame.}

\item{...}{Additional arguments.}

\item{y}{A numeric vector (if \code{x} is a vector).}

\item{poly_degree}{Integer; maximum polynomial degree (default = 3, odd values only).}

\item{monotonicity}{Monotonicity criterion: \code{"relaxed"} (default), \code{"strict"}, or \code{"none"}.}

\item{ties_method}{Tie handling: \code{"average"} (default) or \code{"random"}.}

\item{conf_level}{Numeric in (0, 1) specifying the confidence level, or \code{NULL} for no CI.}

\item{ci_method}{Confidence interval method: \code{"fisher"} (analytical Fisher z) or \code{"bootstrap"}.}

\item{B}{Number of bootstrap resamples (default = 1000).}
}
\value{
An S3 object of class \code{"cor_hermite"} (or \code{"cor_hermite_matrix"}).
}
\description{
Computes the distribution-robust Hermite-Mehler Pearson correlation (\eqn{r_{\mathrm{HM}}}),
the latent Gaussian copula correlation (\eqn{\rho_z}), and the shape attenuation factor (\eqn{A}).
}
